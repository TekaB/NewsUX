{% extends 'base.html.twig' %}

{% block title %}Edit Todo{% endblock %}

{% block body %}
    <turbo-frame id="editTodoPage">
        {{ include('todo/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <turbo-frame id="deleteTodoPage">
            {{ include('todo/_delete_form.html.twig') }}
        </turbo-frame>

        <a href="{{ path('app_todo_index') }}">Annuler</a>
    </turbo-frame>

{% endblock %}
